# Copy to .env and set your values (do not commit .env)

# Database (MariaDB)
# Docker: use host "mariadb". Local: use "localhost"
DATABASE_URL=mysql+pymysql://fastapi_app_user:YOUR_PASSWORD@localhost:3306/fastapi_app_db
